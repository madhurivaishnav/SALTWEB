Javascript.js is depreciated, however, previous publishes will still reference it. 
The solution is to get the javascript.js file to reference the ASPX as an include. 
In other forms of this application, the include can be stripped out and the customised javascript inserted.